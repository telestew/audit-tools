{
  "commands": {
    "lookup_project": "const { plugin_settings_lookup_project: config } =\n  await chrome.storage.local.get(\"plugin_settings_lookup_project\");\nconst days = parseInt(config?.projectDays) || 2;\nconst mode = config?.lookupMode || \"highlighted\";\nlet text =\n  mode === \"clipboard\"\n    ? await navigator.clipboard.readText()\n    : window.getSelection().toString().trim();\nif (text.match(/[A-Za-z0-9]/g) && text.length === 24) {\n  const today = new Date();\n  const formatDate = (d) =>\n    `${d.getFullYear()}-${String(d.getMonth() + 1).padStart(2, \"0\")}-${String(d.getDate()).padStart(2, \"0\")}`;\n  const todayStr = formatDate(today);\n  today.setDate(today.getDate() - (days - 1));\n  const pastStr = formatDate(today);\n  window.open(\n    `https://app.outlier.ai/en/expert/outlieradmin/tools/qc_audit_disputes/${text}?dateRange=${pastStr},${todayStr}`,\n    \"_blank\",\n  );\n} else {\n  alert(\"Not a valid ID: \" + text);\n}\n"
  },
  "config": {
    "lookupMode": "highlighted",
    "projectDays": 2
  },
  "configSchema": [
    {
      "id": "lookupMode",
      "label": "Lookup with",
      "options": [
        {
          "label": "from clipboard",
          "value": "clipboard"
        },
        {
          "label": "from highlighted text",
          "value": "highlighted"
        }
      ],
      "type": "select"
    },
    {
      "id": "projectDays",
      "label": "Project Lookup Days",
      "max": 90,
      "min": 1,
      "type": "number"
    }
  ],
  "enabled": false,
  "id": "lookup-project",
  "name": "Lookup Project"
}
